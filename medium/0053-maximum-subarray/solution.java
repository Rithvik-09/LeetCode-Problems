            maxSum = Math.max(maxSum, currentSum);

            if(currentSum < 0) {
                currentSum = 0;
            }
        }

            currentSum += nums[i];
        for(int i = 0; i < nums.length; i++) {
        int currentSum = 0;

        int maxSum = nums[0];
    public int maxSubArray(int[] nums) {
class Solution {
