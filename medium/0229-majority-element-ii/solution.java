
            if(hm.get(nums[i])== mini){
                res.add(nums[i]);
            }
            hm.put(nums[i],hm.getOrDefault(nums[i],0)+1);
        for(int i=0;i<n;i++){

        int mini = n / 3 + 1;
        HashMap<Integer,Integer> hm = new HashMap<>();

            if(res.size() == 2) break;
        }
        return res;
    }
